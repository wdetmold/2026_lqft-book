% Shared preamble — semantic packages only; CUP applies the book design in production.
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{graphicx}
\usepackage[export]{adjustbox}
\usepackage{amsmath,amsfonts,amssymb}
\usepackage{simpler-wick}
\usepackage{xcolor}
\usepackage{cancel}
\usepackage{slashed}
\usepackage{tikz}
\usetikzlibrary{decorations.markings}
\usetikzlibrary{arrows.meta}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage{algpseudocode}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{comment}
\usepackage[numbers,sort&compress]{natbib}
\usepackage{imakeidx}
\makeindex[intoc]
\usepackage[colorlinks=true,linkcolor=blue!50!black,citecolor=blue!50!black,urlcolor=blue!50!black]{hyperref}

\graphicspath{{figures/notes/}{figures/}}

% ---------------------------------------------------------------------------
% Draft apparatus: TODO notes visible in draft builds
\newcommand{\todo}[1]{\par\noindent\textcolor{red!70!black}{\textbf{TODO:} #1}\par}

% ---------------------------------------------------------------------------
% Exercises: statement always shown; solutions shown only when \solutionstrue
% (make solutions). Numbered per chapter.
\newif\ifsolutions
% \solutionstrue is set via \IfFileExists on solutions-flag (written by Makefile)
\IfFileExists{./solutions-flag.tex}{\input{./solutions-flag.tex}}{}

\newcounter{problem}[chapter]
\newcommand{\problem}[2]{%
  \par\medskip\hrule\vspace{2mm}%
  \refstepcounter{problem}%
  {\noindent\textbf{Exercise \thechapter.\arabic{problem}:} #1\par}%
  \ifsolutions
    {\medskip\noindent\textbf{Solution:} #2\par}%
  \fi
  \vspace{2mm}\hrule\medskip}

% ---------------------------------------------------------------------------
% Figure-redraw review: original sketch beside proposed redraw. Once a redraw
% is approved, replace \redrawcompare{old}{new} by \includegraphics{new}.
\newcommand{\redrawcompare}[2]{%
  \begin{minipage}[c]{0.48\linewidth}\centering
    \includegraphics[width=\linewidth]{#1}\\[1mm]{\scriptsize notes original}
  \end{minipage}\hfill
  \begin{minipage}[c]{0.48\linewidth}\centering
    \includegraphics[width=\linewidth]{#2}\\[1mm]{\scriptsize proposed redraw}
  \end{minipage}}

% ---------------------------------------------------------------------------
% Automatic index entries (inserted by tools/apply_index.py from
% refs/index-vocabulary.tsv). Hand-written entries use \index directly.
\newcommand{\aidx}[1]{\index{#1}}

% ---------------------------------------------------------------------------
% Draft flags: material drafted mechanically and awaiting the author's check.
%   \problemdraft[note]{statement}{solution}
%       an exercise not yet verified. To ACCEPT it, delete "draft" from the
%       command name and drop any [note] -- a one-word edit.
%   \unverified{...}
%       inline marker for one specific claim inside otherwise checked material.
% \draftflagsfalse typesets a clean copy; tools/list_draft_flags.py reports
% everything still outstanding, and `make lint` counts them.
\newif\ifdraftflags \draftflagstrue
\definecolor{draftflagcolour}{RGB}{176,72,0}
\newcommand{\draftflagtag}[1]{%
  \ifdraftflags
    {\footnotesize\sffamily\color{draftflagcolour}%
      [\,unverified draft --- author check pending%
       \if\relax\detokenize{#1}\relax\else: #1\fi\,]}%
    \par\nobreak
  \fi}
\newcommand{\problemdraft}[3][]{\problem{\draftflagtag{#1}#2}{#3}}
% The [check] tag has to survive being used inside an equation, so route it
% through \text{} in math mode.
\newcommand{\checktag}{%
  \ifmmode\text{\footnotesize\sffamily[check]}\else{\footnotesize\sffamily[check]}\fi}
\newcommand{\unverified}[1]{%
  \ifdraftflags{\color{draftflagcolour}#1\,\checktag}\else#1\fi}

%   \remark{title}{text} / \remarkdraft[note]{title}{text}
%       a set-off aside in the main text.  The "draft" form carries the same
%       author-check tag as \problemdraft; accept it by deleting "draft" from
%       the command name (and any [note]).
\newenvironment{remarkblock}{%
  \begin{list}{}{\setlength{\leftmargin}{1.4em}\setlength{\rightmargin}{1.4em}%
                 \setlength{\listparindent}{\parindent}%
                 \setlength{\itemindent}{0pt}\setlength{\topsep}{\medskipamount}}%
  \item[]\small}{\end{list}}
\newcommand{\remark}[2]{%
  \begin{remarkblock}\textbf{#1}\enspace #2\end{remarkblock}}
\newcommand{\remarkdraft}[3][]{\remark{#2}{\draftflagtag{#1}#3}}

% ---------------------------------------------------------------------------
% Identity matrix.  NOTE: \mathbb{1} is NOT valid with amssymb -- the msbm font
% has no digits, so \mathbb{1} silently typesets as the logic symbol U+22AE.
% Use a proper blackboard-bold one where the font is available, and fall back to
% an upright bold 1 otherwise, so the manuscript is portable.
\IfFileExists{bbm.sty}{%
  \usepackage{bbm}\newcommand{\Id}{\mathbbm{1}}}{%
  \IfFileExists{dsfont.sty}{%
    \usepackage{dsfont}\newcommand{\Id}{\mathds{1}}}{%
    \newcommand{\Id}{\mathbf{1}}}}
